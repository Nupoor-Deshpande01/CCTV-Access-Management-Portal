rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /footage/{requestId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && resource == null
                   && request.resource.size < 500 * 1024 * 1024;
    }
  }
}
